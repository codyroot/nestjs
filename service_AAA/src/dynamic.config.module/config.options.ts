export interface ConfigOptions {
    type: CONFIG_OPTIONS;
}

export enum CONFIG_OPTIONS {
    DEV = "DEV",
    PROD = "PROD",
}

export const CONFIG_OPTIONS_IDENTIFIER = "CONFIG_OPTIONS_IDENTIFIER";
